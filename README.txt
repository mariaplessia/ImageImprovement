The operating system used to implement this program was mac OS - M1. The IDE used to write the code was VSCode, and it was ran from terminal using the following command:

For the default image (pcb.png):
		python3 ps3-1.py
For the rest of the images (e.g. rainbow.png)
        python3 ps2-1.py -i rainbow.png
